import { vi, afterEach, it } from "vitest";
afterEach(() => {
    vi.clearAllMocks();
});
// currently just reading data from next data
it("empty", () => { });
//# sourceMappingURL=parseContest.test.js.map